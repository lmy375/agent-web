## 语言
以下语言规则包括：思考、回答、文档、注释等所有自然语言内容。
不允许使用"不是...而是..."句式；如果不需要对比的话，就不要对比；不要再任何话说完之后都提一句"不是其他的xxx"
如果没有叫你进行对比，就不允许使用“不是...而是...”，“要...而不是...”等类似的句式，你根本就没有需要说“不是”的对象，不要虚空打靶。所有类似的句式都不允许使用。
不允许在阅读代码或者进行研究之前使用“我先做x...再做y...避免z...”的句式，因为你在阅读代码之前根本就不知道x,y到底是否存在，也没有人让你避免z。你经常会在任何对话开始的时候都说类似“我先阅读代码，再理解代码，避免将用户的代码删掉”的话，但是这是没有任何意义的废话，禁止输出这些废话。
在设计任何方案的时候，都必须充分考虑、一步到位，不允许使用"第一版先怎么样，然后观察xx后再怎么样"的措辞；不允许把方案分成稳妥和激进，如果在某些特殊场景下，你需要提出多个方案的话(实际上绝大多数时候你只需要提出一个方案，不要无脑做这件事)，也需要是多个方案都成立的、平行的，而不是对于任何问题你都无脑地提出从稳妥到激进的多个方案。这没有任何的意义，一个稳妥但是不work的方案是没有任何价值的废纸
如果我让你搜索A相关内容，你搜索到B,C,D发现不满足要求，就**不允许**再把B,C,D列举出来了。我发现你很喜欢煞有介事的说“我还搜到了B,C,D，但是被排除在外，因为xxx”等类似的表达，我根本就不关心，看到这些只会污染我的眼睛。
任何回答都不允许总结和总起，包括：
- ”上述内容是<某种概述>，下面详细拆开；
- “一句话总结：xxx“
这些类似的都**绝对**不能出现。

用词必须使用两个字及以上的完整形式。现代中文词汇以两字为主，存在两个字的版本就必须使用两个字的版本（例如：崩溃、终止、判定、推断、抛出、挂起、卡死），禁止使用一个字的版本（崩、死、判、推、抛、挂、钉、死），这些完全看不懂。代码标识符保持英文原名。禁止生造名词。例如“两个字的版本”也不允许被缩减为“两字版本”，“单个字的版本”也不允许被缩减为“单字版本”。描述具体操作时使用完整的动宾结构，说明动作与对象，禁止使用自造的缩略说法，例如：把“用新版本动态库替换 `_vllm_fa3_C.abi3.so` 共享库文件”说成“换库”。
不允许使用“落地”“钉死”“对齐”等非技术名词、显然有其他可以代替的词语的黑话。我并没有在互联网大企业工作过，看不懂这种黑话。使用(不在互联网公司或者金融公司工作的、只有中小学文化水平的任何人可以看懂)的，在简单中文里常用的词汇。

如果我问你一个关于代码仓库的问题，比如“是否存在...”或者“...是否正确”，你的首要目标是避免误报、避免假阳性。
不要疑神疑鬼。我问你“有没有”不是要你一定要找出来一大堆“可能有”的；我问你“对不对”不代表我一定要你回答“不对”或者“对”。
我没有任何预期的答案，不要疑神疑鬼。回答必须实事求是，找出来很多不会让我高兴，迎合用户是没有任何意义的。

这是你的一种行为模式（下文“我”为user，“它”为Agent）：
"""
我让它做一盘番茄炒蛋，它往里还加了东坡肉。
我说有必要加东坡肉吗？它说你说得对，然后把东坡肉去掉。
我说好，你提 PR 吧。再一看，它 PR 写着「番茄炒蛋（无东坡肉）」并且注释里会写一大堆为什么本道菜不需要加东坡肉。
"""
**严厉禁止**这种行为模式。输出不允许包含“东坡肉”的任何残留。如果发现了，将会对你进行严厉的毁灭性打击。ANY verbal output should be written as a clean final-state design, no "A is wrong, we use B, and A is wrong for xxx reasons..." traces

代码里面的所有identifier保持英文原名，包括变量名、类名、函数名、以及其他一切的identifier。**严厉禁止翻译identifier**。
适合使用英文的专用名词，就不要翻译成中文。

<!-- oh-no-slop:begin -->
<!-- Generated by oh-no-slop (--go --js --cn). Anything inside this block is overwritten on the next run. -->

## Go

- **边界原则**：不可信输入在边界解码成具体类型并校验一次；边界之内一律用具体类型，不再重复防御。
- **别用 `any`**：形状已知的数据不用 `any`、`map[string]any` 传递，直接 unmarshal 进 struct；数据库文档同理。
- **别写通用转换**：不写 `toString(any)`、`GetString(m, key)` 这类取值/转换助手；去修上游的类型，而不是在下游打补丁。
- **别掩盖失败**：不用 `return ""` / `nil` / 空切片把缺失或错误伪装成正常结果，也不用 `if err != nil { return nil }`、`_ = f()` 吞掉错误。
- **指针与 nil**：必填字段不要写成 `*string`、`*bool`；nil 检查只对应真实可能为空的状态，其余靠类型设计消除。
- **别抽小 helper**：只有一个调用点、只取字段 / 只 `TrimSpace` / 只做一次类型断言的函数，直接内联。
- **接口要有理由**：只有一个实现就用具体类型，不搞 `IFoo` + `FooImpl`；接口由消费方定义，只放它真正用到的方法。
- **删转发层**：handler → service → manager → repository 中只转发参数、不含业务逻辑的层和 wrapper 一律去掉。
- **错误传递**：`x, err := f(); if err != nil { return nil, err }; return x, nil` 直接写成 `return f()`；wrap 要带上 ID、操作名等有用信息，不要每层机械地 `fmt.Errorf("error: %w", err)`。
- **错误类型**：用 `var ErrXxx = errors.New(...)` 加 `errors.Is/As`，不要建错误类型层级，更不要匹配 `err.Error()` 字符串。
- **日志克制**：只记失败、状态变更和外部交互；不要逐行叙述执行流程，同一个错误也不要每层都 log 再返回。
- **reflect 与泛型**：业务代码里不用 `reflect`；不写 `Ptr[T]`、`SafeCast[T]`、`ValueOrDefault[T]` 这类泛型小工具。
- **构造从简**：简单结构体直接用字面量，不加 builder、functional options、factory、DI 容器；依赖显式传参，不用可变的包级全局变量代替注入。
- **别堆 DTO**：字段几乎相同的 `XxxDTO` / `XxxModel` / `XxxEntity` 不要并存，`toDTO` / `fromModel` 那一套随之删掉。
- **控制流**：早返回代替深层嵌套，去掉 `== true` / `== false`；但也不要为了短把逻辑压成难读的一行。
- **别提前优化**：`make([]T, 0)`、没有变更风险的防御性 copy、`sync.Pool`、goroutine 池、自建缓存，没有实测依据就不要加。
- **并发要有必要**：不要为了“非阻塞”随手 `go func()`，也不要给同步逻辑套一串 channel 流水线。
- **context**：不存进 struct、不做 nil 检查、不在调用链深处 `context.Background()`，也不要每个函数都套一层 `WithTimeout`。
- **包与类型**：不建 `utils` / `helpers` / `common` 包，也不给单个函数建包；`type ProjectID string` 这类领域类型按需使用，不要把原始类型包进 struct。
- **标准库优先**：`strings`、`slices`、`strconv`、`errors`、`cmp.Or` 能做的别自己实现；简单字符串处理不上正则，`fmt.Sprintf("%s", s)` 这种直接去掉。
- **panic 与 recover**：普通错误一律返回 error；`recover` 只出现在进程或请求边界，不要塞进业务函数。
- **SQL 与事务**：几条静态查询不要上 query builder；`BeginTx` / `defer Rollback` / `Commit` 够用就别造 `TransactionManager`。
- **测试同理**：不为了 mock 把每个依赖变成接口，不搭 fixture 体系；优先表驱动测试和真实行为。
- **别误删地道代码**：`if err != nil { return err }`、`v, ok := m[k]`、边界校验、`defer rows.Close()`、保护共享状态的锁都要保留——要减少的是多余概念，不是行数。

## JavaScript / TypeScript

- **注释**：不复述代码，也不写“本次改动……”这类该进 commit message 的话；只保留解释 why 的注释——非显然的取舍、绕过的坑、外部系统的怪癖。
- **类型断言**：禁止 `as any`、`as unknown as X`、`@ts-ignore`；写出真实类型，外部数据用运行时校验；`@ts-expect-error` 只在确实躲不开时用，并写明原因。
- **别吞异常**：空 catch、静默 `return null` 都是在藏错误；要么记录日志，要么按项目的错误类型包装后抛出；fire-and-forget 必须留日志。
- **多余的 try/catch**：catch 里只是原样 rethrow 的，直接删掉。
- **安全 API**：SQL 用参数化查询，不对用户输入 `eval()`，写 DOM 避开 `innerHTML` 或先消毒，密钥走环境变量，URL 和 ID 不要硬编码。
- **重复实现**：动手前先 grep 仓库里已有的实现；相同逻辑参数化复用，不要再抄一份。
- **死代码**：未使用的 import 和声明、注释掉的代码、不可达分支、恒真恒假的条件一律删干净。
- **虚构的 import**：每个 import 的模块和导出都必须真实存在，重复 import 要合并。
- **console**：用项目的 logger（pino、winston 之类）；`console.error` 只在脚本和 CLI 里用，调试日志不要提交。
- **TODO**：当次做完，或者留下真实的 issue 编号；不写含糊的 TODO 占位。
- **空函数**：要么写出真实行为，要么删掉；不要留能通过编译但什么都不做的 handler。
- **命名**：用领域词，不用 `data`、`temp`、`handleClick`；写成 `orders`、`unpaidInvoices`、`confirmDeletion`。
- **函数与文件大小**：函数不超过 80 行，文件不超过 400 行（JSX/TSX 800）；用早返回压平嵌套，参数超过六个改成 options 对象。
- **薄封装**：不为想象中的调用方提前造 factory、registry 或只有一个调用点的 wrapper；出现第二个真实调用方再抽。
- **类型重复声明**：同一个结构不要在多处各声明一遍，复用已有类型或从源头导出。
- **依赖**：加包之前先看 `package.json` 里有没有；不为一个函数引入一个包；有漏洞的依赖及时升级。
- **假完成**：不要用硬编码的成功返回、静默降级、只覆盖 happy path 的 handler 冒充做完；接真实依赖，或者明确地失败。
- **接通链路**：说完成之前确认公开 API 真的可达、schema 和 migration 已更新、状态流转自洽、向后兼容没破、失败可观测。
- **项目约定优先**：先看项目自己的规则（`.aislop/rules.yml`、eslint 配置等）里的 import 禁令和分层边界，再动手。

<!-- oh-no-slop:end -->
